[project]
name = "gazebo-mcp"
version = "0.2.0"
description = "General-purpose Gazebo simulator wrapper via MCP"
requires-python = ">=3.11"
dependencies = ["fastmcp>=3.2", "httpx>=0.28", "fastapi>=0.115", "uvicorn[standard]>=0.34"]

[project.scripts]
gazebo-mcp = "gazebo_mcp:main"
