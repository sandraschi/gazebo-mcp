# gazebo-mcp — TODO

## Future Ideas

- [ ] **Chat personality selector** — Multiple LLM personas with localStorage persistence
- [ ] **Chat history persistence** — Save/load conversations across page reloads
- [ ] **MCP resources + prompts** — Register `@mcp.resource("world://{name}")` and `@mcp.prompt()` templates
- [ ] **MCPB bundle** — Rebuild `dist/gazebo-mcp-v0.2.0.mcpb` with latest source
